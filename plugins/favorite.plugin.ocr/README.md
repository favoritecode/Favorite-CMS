# Favorite OCR Tool

Inactive by default. Approve `permission.register` and `tool.register`, activate it, and explicitly grant `favorite.plugin.ocr.execute` to a role. Configure the separate Worker before use. Only operator-allowlisted HTTPS image URLs are accepted. Private CMS Media requires a future signed Media-delivery contract. Public anonymous execution remains disabled until a rate-limit/quota/abuse-control contract exists.
