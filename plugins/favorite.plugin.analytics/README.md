# Favorite Analytics Integration
Supports safe `none` and `first-party` presentation states. It accepts no script URL, remote provider, credential, or secret and performs no network request by itself.
